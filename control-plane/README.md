# Control Plane

Provider-neutral services for machine inspection, resource planning/admission,
environment lifecycle, cache/transfer management, durable recoverable local
state, lifecycle events, CLI/API surfaces, and hard containment live here.

This scaffold deliberately selects no implementation language, package format,
provider, or daemon strategy. Local state remains operational and non-canonical.
